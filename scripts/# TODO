Fix issue with firmware updates that does not always work on the first try.
REmove Piping warning message.

REduce the image size

Install SAMD Arduino Package

Update rpi3 serial script to delete it self before restart. Give notice of restart

Add 
https://github.com/Denvi/Candle/blob/master/readme.md
